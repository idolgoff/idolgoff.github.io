/**
 * Created by dolgov.ivan@gmail.com on 17.04.2018.
 *
 * Filter
 * -- filter: Object
 *
 */

export const filter = (state = {}, action) => {
    switch (action.type) {
        default:
            return state;
    }
};